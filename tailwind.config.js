const colors = require('tailwindcss/colors')

module.exports = {
  purge: [],
  darkMode: false, // or 'media' or 'class'
  theme: {
    colors:{
      Cyan: colors.cyan,
      lightBlue: colors.lightBlue
    },
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
}
